# dependencies
node_modules/
.pnp
.pnp.js

# next
.next/
out/
build/

# env — ignore every real env file; only .env.example is committed
.env
.env.*
!.env.example

# local lead/admin store (populated at runtime; on Vercel lives in /tmp)
data/

# misc
.DS_Store
*.pem
.vscode/
.idea/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts
.vercel

# claude — local-only project instructions (auto-loaded by Claude Code)
CLAUDE.local.md
.claude.md

# large media — host on Vercel Blob / CDN / Vimeo instead of git
media/
