// Canonical production URL. Used for canonical tags, OG URLs, sitemap, JSON-LD.
// Vercel preview URLs are intentionally NOT used here — every preview points to
// the same canonical so Google never indexes preview hosts and social cards stay
// consistent regardless of where the build is deployed.
export const SITE_URL = 'https://visionair.biz.pl';
